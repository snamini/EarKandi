import React from 'react';


export default class MyFooter extends React.Component {
  render(){
    return (
// make a call to the server to get your login status
//
// if you are logged in, you can render your routes one way. otherwise render them the other way.

// component will mount
//
// on component mount, go retrieve data
//
// if component was updated, figure out whether it was them becoming logged out or logged in

);
}
}
